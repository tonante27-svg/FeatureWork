<apex:page controller="AccountListControllers" >
   <apex:pageBlock title="sObject List Example">
       <apex:pageBlockTable value="{!accounts}" var="acc">
           <apex:column value="{!acc.Name}"/>
           <apex:column value="{!acc.Id}"/>
         </apex:pageBlockTable>
   </apex:pageBlock>
</apex:page>